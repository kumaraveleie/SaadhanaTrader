"""Sector-strength aggregator package.

Hosts the M1-v0 module per ``spec/thinking_engine.md`` §3.1 — a
deterministic sector-strength snapshot built from existing OHLCV
data, intended as the v0 surface that ships with K1. The full M1
classifier (Phase Q) replaces this v0 with a richer formulation
once Phase D's catalyst data and Phase R's lifecycle classifier
land.
"""
